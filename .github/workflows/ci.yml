name: Python App CI

on:
    workflow_dispatch:
    push:
        paths:
            - main.py
            - Dockerfile
            - requirement.txt
            - ci.yml
        branches:
            - main

jobs:
    build-test:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Repo
              run: echo ""

            - name: Setup Python
              run: echo ""

            - name: Install Dependencies
              run: echo ""

            - name: Run Tests
              run: echo ""

    docker-build-push:
        runs-on: ubuntu-latest
        needs: build-test
        steps:
            - name: Checkout Code
              run: echo ""

            - name: Login to Docker Hub
              run: echo ""

            - name: Push Image to Docker Hub
              run: echo ""
